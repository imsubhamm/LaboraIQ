export default function SettingsPage() { return <section><div className="page-heading"><div><p className="eyebrow">ENVIRONMENT</p><h1>Settings</h1><p>Runtime and security configuration is controlled outside application source.</p></div></div><div className="panel settings-list">{[["Identity provider","Development adapter","OIDC / Cognito / Entra compatible"],["Session duration","60 minutes","Environment controlled"],["API version","v1","Stable platform contract"],["Data region","Local development","AWS region selected at deployment"]].map(([a,b,c])=><div key={a}><span><strong>{a}</strong><small>{c}</small></span><b>{b}</b></div>)}</div></section>; }

